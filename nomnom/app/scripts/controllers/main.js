'use strict';

/**
 * @ngdoc function
 * @name nomnomApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the nomnomApp
 */
angular.module('nomnomApp')
  .controller('MainCtrl', function ($scope, apiService) {
    // $scope.awesomeThings = [
      // 'HTML5 Boilerplate',
      // 'AngularJS',
      // 'Karma'
    // ];	
	
	// apiService.test().then(function(data){
	
		// $scope.test  =  data;
	// });
	
  });
